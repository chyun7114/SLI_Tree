# Vue Component Guidelines

## 기본 원칙

- 페이지는 `App.vue`에서 큰 레이아웃만 조립하고, 실제 UI 단위는 `src/components` 아래의 작은 컴포넌트로 분리한다.
- 컴포넌트 하나는 한 가지 책임만 가진다. 예를 들어 히어로, 링크 카드, 섹션 리스트처럼 사용자가 인식하는 UI 단위를 기준으로 나눈다.
- 같은 마크업 패턴이 2번 이상 반복되면 props 기반 컴포넌트로 추출한다.
- 컴포넌트 이름은 PascalCase를 사용하고, 파일 이름도 컴포넌트 이름과 동일하게 유지한다.

## Props와 상태

- props 타입은 `interface` 또는 `type`으로 명시하고, 배열/객체 데이터는 가능한 한 상위 컴포넌트에서 선언해 전달한다.
- 로컬 상태는 그 컴포넌트 안에서만 의미가 있을 때만 둔다. 여러 컴포넌트가 공유하면 상위로 끌어올린다.
- emit 이름은 사용자 행동을 설명하는 동사형으로 작성한다. 예: `select`, `submit`, `toggle`.

## Tailwind CSS 사용 규칙

- 레이아웃, 간격, 색상, 타이포그래피는 Tailwind 유틸리티 클래스를 우선 사용한다.
- 반복되는 시각 패턴은 컴포넌트로 추출한다. 단순히 긴 class 문자열을 숨기기 위한 전역 CSS 클래스는 만들지 않는다.
- 전역 CSS는 `src/style.css`에 두며, 앱 전체 토큰, base 스타일, Tailwind로 표현하기 어려운 장식 요소만 포함한다.
- 조건부 스타일은 Vue의 `:class` 바인딩을 사용하고, 동적인 색상 문자열을 런타임에 조합하지 않는다.

## 파일 배치

- 페이지 전체 조립: `src/App.vue`
- 재사용 UI 컴포넌트: `src/components/*.vue`
- 정적 이미지/아이콘 import: `src/assets`
- 전역 CSS와 Tailwind import: `src/style.css`

## 체크리스트

- 새 UI가 `App.vue`에 과도하게 쌓이지 않았는가?
- 반복되는 카드, 버튼, 리스트 구조가 컴포넌트로 분리되어 있는가?
- props 타입이 명확하고 컴포넌트 책임이 작게 유지되는가?
- Tailwind 유틸리티가 스타일의 기본 수단으로 사용되고 있는가?
